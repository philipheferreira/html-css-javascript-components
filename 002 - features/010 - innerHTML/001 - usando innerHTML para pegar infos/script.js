// 1. Selecionamos o elemento pelo ID
const elemento = document.getElementById('minhaCaixa');

// 2. Pegamos o conteúdo HTML interno dele
const conteudo = elemento.innerHTML;

// 3. Exibimos no console
console.log(conteudo);